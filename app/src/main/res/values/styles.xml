<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/c_ffffff</item>
        <item name="colorPrimaryDark">@color/c_ffffff</item>
        <item name="colorAccent">@color/c_638ee3</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/main_bg</item>
        <item name="android:textColorSecondary">#333333</item>
    </style>
    <!-- 仿IOS底部弹框 -->
    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/ycdialog_photochoose_bg</item>
    </style>

    <!-- 猫加载框样式 -->
    <style name="cart_dialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>


    <!-- 顶部弹出动画 -->
    <style name="TopAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_top_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_top_out</item>
    </style>

    <!-- 底部弹出动画 -->
    <style name="BottomAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_bottom_out</item>
    </style>

    <!-- 缩放动画 -->
    <style name="ScaleAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_scale_out</item>
    </style>

    <!--圆角imageview-->
    <declare-styleable name="ChangeGasStationImageView">
        <attr name="ltop" format="dimension"></attr>
        <attr name="rtop" format="dimension"></attr>
        <attr name="lbottom" format="dimension"></attr>
        <attr name="rbottom" format="dimension"></attr>
    </declare-styleable>


    <attr name="selectedColor" format="color"/>
    <attr name="unselectedColor" format="color"/>
    <declare-styleable name="Indicator">
        <attr name="selectedColor"/>
        <attr name="unselectedColor"/>
    </declare-styleable>

</resources>